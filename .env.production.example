#--------------------------------------------------------------------------
# PRODUCTION – Copy vào .env và chỉnh lại theo server của bạn
# cp .env.production.example .env && nano .env
#--------------------------------------------------------------------------

#--------------------------------------------------------------------------
# Môi trường & Debug (bắt buộc cho production)
#--------------------------------------------------------------------------
APP_NAME=Bault
APP_ENV=production
APP_DEBUG=false
APP_KEY=base64:XXXX   # Chạy: php cli make:key hoặc php -r "echo 'base64:'.base64_encode(random_bytes(32));"
APP_URL=https://your-domain.com
VITE_DEV_URL=

#--------------------------------------------------------------------------
# Log: production chỉ ghi warning trở lên
#--------------------------------------------------------------------------
LOG_CHANNEL=default_stack
LOG_LEVEL=warning

#--------------------------------------------------------------------------
# Session: bật Secure khi dùng HTTPS
#--------------------------------------------------------------------------
SESSION_SECURE_COOKIE=true
SESSION_SAME_SITE=lax

#--------------------------------------------------------------------------
# CMS: tắt auto sync blocks trên mỗi request (đã build cache module-blocks)
#--------------------------------------------------------------------------
CMS_AUTO_SYNC_BLOCKS=false

#--------------------------------------------------------------------------
# Swoole: chạy nền, log ít
#--------------------------------------------------------------------------
SWOOLE_DAEMONIZE=true
SWOOLE_LOG_LEVEL=3

#--------------------------------------------------------------------------
# Các biến khác giữ từ .env.example (DB, Redis, Cache, Mail, …)
# Chỉnh DB_*, REDIS_*, MAIL_*, APP_URL, RPC_SECRET_TOKEN cho đúng production.
#--------------------------------------------------------------------------
